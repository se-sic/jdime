plugins {
    id("org.gradle.toolchains.foojay-resolver-convention") version "1.0.0"
}

rootProject.name = 'JDime'
rootProject.buildFileName = 'build.gradle.kts'

if (!ext.JNM_MAVEN.toBoolean()) {
    includeFlat('JNativeMerge')
}
